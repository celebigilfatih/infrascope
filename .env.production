# ============================================================================
# InfraScope - Production Environment Configuration
# DO NOT commit this file to version control
# Generate secure values and keep this private
# ============================================================================

# Database Configuration
# ⚠️ Use strong password and secure hostname/network
DATABASE_URL="postgresql://{DB_USER}:{DB_PASSWORD}@{DB_HOST}:5432/{DB_NAME}"
DB_USER="{CHANGE_ME}"
DB_PASSWORD="{GENERATE_STRONG_PASSWORD}"
DB_NAME="infrascope"
DB_HOST="{YOUR_DB_HOST}"
DB_PORT="5432"

# Next.js Configuration
NODE_ENV="production"
NEXT_PUBLIC_API_URL="http://localhost:3170"
PORT="3000"

# Authentication (NextAuth.js)
# ⚠️ Generate a random secret using: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3170"
NEXTAUTH_SECRET="{GENERATE_USING_OPENSSL}"

# Application Settings
APP_ENV="production"
LOG_LEVEL="info"

# Telemetry
NEXT_TELEMETRY_DISABLED="1"

# Redis Configuration (if enabled)
REDIS_URL="redis://:PASSWORD@redis-host:6379"
REDIS_PASSWORD="{GENERATE_STRONG_PASSWORD}"

# Agent API Configuration
AGENT_API_TOKEN="{GENERATE_SECURE_TOKEN}"
AGENT_API_PORT="8080"

# Security Settings
STRICT_MODE="true"

# ============================================================================
# Generation Commands:
# 
# Generate NEXTAUTH_SECRET:
#   openssl rand -base64 32
#
# Generate REDIS_PASSWORD:
#   openssl rand -base64 24
#
# Generate AGENT_API_TOKEN:
#   openssl rand -hex 32
#
# ============================================================================
# 
# Deployment Checklist:
# ☐ Replace all {PLACEHOLDER} values with actual production values
# ☐ Use strong, randomly generated passwords
# ☐ Set correct DATABASE_URL with production database
# ☐ Set NEXTAUTH_URL to your production domain
# ☐ Set NEXT_PUBLIC_API_URL to your production domain
# ☐ Keep this file in secure storage, not in git
# ☐ Use Docker secrets or environment variable injection
# ☐ Regularly rotate NEXTAUTH_SECRET and API tokens
#
# ============================================================================
